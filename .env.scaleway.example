# Scaleway Environment Configuration Template
# Copy this to .env.scaleway and fill in your values
# DO NOT commit .env.scaleway to git!

# Scaleway API Credentials (from IAM → API Keys)
SCW_ACCESS_KEY=your-scaleway-access-key
SCW_SECRET_KEY=your-scaleway-secret-key

# Scaleway Project/Organization
SCW_DEFAULT_PROJECT_ID=your-project-id
SCW_DEFAULT_ORGANIZATION_ID=your-organization-id

# Deployment Configuration
SCW_REGION=fr-par              # Options: fr-par, nl-ams, pl-waw
SCW_BUCKET_NAME=synflow-prod   # Must be globally unique

# Optional: Custom domain configuration
CUSTOM_DOMAIN=synflow.org      # Your custom domain (if applicable)
