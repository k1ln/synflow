// Legacy duplicate test file kept intentionally empty (logic moved to .tsx variant).
import { describe, it, expect } from 'vitest';
describe('IIRFilterFlowNode duplicate placeholder', () => {
  it('noop', () => {
    expect(true).toBe(true);
  });
});
